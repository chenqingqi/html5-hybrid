		/**
		 * ajax跨域，不同域名之间的ajax数据交互是被默认禁止的，解决这个问题的最好方式是在服务端设置可被访问的白名单
		 */
		
		<-- server.php -->
		
		// 指定允许其他域名访问  
		header('Access-Control-Allow-Origin:*'); 
		
		// 响应类型  
		header('Access-Control-Allow-Methods:POST');  
		
		// 响应头设置  
		header('Access-Control-Allow-Headers:x-requested-with,content-type');